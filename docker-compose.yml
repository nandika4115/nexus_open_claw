version: "3.9"
services:
  nexus:
    build: .
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - nexus_memory:/root/.nexus
    restart: unless-stopped
    ports:
      - "8080:8080"
volumes:
  nexus_memory:
